contract Wallet {
  var owner: Address
  var contents: Ether
}

Wallet :: (any) {
  func factorial(n: Int) -> Int {
    if n < 2 {
      return 0;
    } else {
      return n * factorial(n - 1);
    };
  }
}
