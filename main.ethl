contract Wallet {
  var owner: Address
  var contents: Ether
}

Wallet :: (any) {
  func factorial(n: Int) -> Int {
    if n < 2 { return 0 } 

    return n * factorial(n - 1)
  }

  func foo() {
    if true { print("error"); return; }
  }
}
