contract Bank {
  var manager: Address
  var accounts: [Address: Int]
  var someAccount: Address
}

Bank :: (any) {
  public mutating func deposit(account: Address, amount: Int) {
    accounts[account] = accounts[account] + amount
  }

  public mutating func setManager(manager: Address) {
    self.manager = manager
  }

  public mutating func setSomeAccount(someAccount: Address) {
    self.someAccount = someAccount
  }
}

Bank :: (manager) {
  public mutating func clear(account: Int) {
    accounts[account] = 0
  }
}

Bank :: account <- (someAccount) {
  public func getBalance() -> Int {
    return accounts[account]
  }
}
