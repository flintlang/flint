// detect malformed contract declaration but be able to recognise that Iden is still a contract
contrwct Iden {
  let x : Int = "sdfsd"
  var z : String = 0
}

// This entire contract declaration will now be a valid behaviour declaration
// I should be able to detect any semantic errors that appear 
Iden :: (any) {

  public init() {}

  func test(t : Int, t1: Int) -> Int {
    return t
  }

  func test_one() -> Int {
     // semantic error
     return test(1)    
  }
}


