contract Iden {
}

// This entire contract declaration will be an error
Iden :: (any) {

  public init() {}

  func test_one() -> Int {
     // the IDE should highlight that the variable x is being used in a immutable function but being used in a mutable way
     var x : Int = 0
     return x  
  }
}

