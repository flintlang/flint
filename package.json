{
  "name": "flint",
  "version": "0.0.1",
  "homepage": "https://github.com/flintlang/flint/",
  "license": "MIT",
  "scripts": {
    "postinstall": "cd utils/codegen && npm install && cd ../gasEstimator && npm install && cd ../coverage && npm install && cd ../testRunner && npm install && cd ../.."
  },
  "dependencies": {
    "codegen": "file:utils/codegen",
    "mkdirp": ">=0.5.1",
    "nunjucks": ">=3.1.3",
    "solc": "^0.4.25",
    "web3": "^0.20.7",
    "why-is-node-running": "^2.1.0"
  }
}
