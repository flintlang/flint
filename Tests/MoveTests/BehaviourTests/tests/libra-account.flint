contract Account {
  visible var value: LibraCoin
}

Account :: sender <- (any) {
  public init() {
    value = LibraCoin()
  }

  public func getBalance() -> Int {
    return value.balance()
  }

  func transfer(to: inout LibraCoin, n: Int) mutates (value) {
    value.transfer(to: &to, amount: n)
  }
}
