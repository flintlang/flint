import AST

public class GasEstimator {
    
    public init() {}
    
    public func estimateGas(ast : TopLevelModule) -> String {
        // I have to add the code which takes a contract and estimates all of the gas costs for the method
        return ""
    }
    
}
